# 2. 待落地优化 Todo（决赛活动队列）

> **唯一实施方案**：[§14 决赛优化状态与实施验证总表](./14.决赛优化状态与实施计划.md)。本文只保留活动队列，不再收纳已证伪或无触发证据的想法。
> **正确性冻结版本**：`bc2c208`（H3 源码 `d39fcdb`）官方全 PASS；最新中位 **34,592**。**性能回退锚点**仍为 `c524ff8` **37,248.4 NewOrder/min**。
> 本地 W=5 仅用于同机趋势，不能替代 W=50 决赛形验收。

---

## 0. 队列规则

| 状态 | 含义 |
|:---:|------|
| 🔴 | 正确性门禁，性能工作前完成 |
| 🟠 | 已有当前瓶颈证据，允许实施 |
| 🧪 | 先测量，满足触发门槛后才能改代码 |
| ⏸ | 未触发，计划见 §14，但不进入近期排期 |
| ❌ | 已从活动队列清退，见 [§6](./6.搁置与低优先级方案.md) |

一次只推进一项；正确性失败、运行期 ERROR、恢复差异或索引漏行均直接回退，不以吞吐抵扣。

---

## 1. 当前活动项

| 顺序 | ID | 任务 | 当前状态 | 完成定义 |
|:---:|:---:|------|:---:|----------|
| — | C-H3 | INLJ 补 SSI 内表谓词/RID 读跟踪 | 🧊 | `bc2c208` 官方全 PASS 后冻结；仅新 SER Join 反例解冻 |
| 1 | C-H4 | `ser_finish` 使用锁内计算的 watermark | 🔴 | 不再无锁读 `active_rts_`；TSan/H4 高重复通过 |
| 2 | P-A1 | abort 事务族×stmt×reason 归因 | 🟠 | Top-3 原因覆盖 ≥80%，两次 W=50 形分布稳定 |
| 3 | P-A2 | 热点 district/stock 冲突窗口收缩 | 🧪 | 仅在相关原因占 abort ≥30% 或贡献 p99 ≥25% 后启动 |
| 4 | P-A3 | PREPARE_SET 分析/元数据缓存 | 🧪 | P-A1 证明持锁后解析/计划开销显著后，按单语句形态落地 |
| 5 | P-W1 | 字节/等待者阈值组提交 | 🧪 | 仅当连续三窗 `commit_waits/fsync≤2` 且 commit p95 占比 ≥20% |

详细实现步骤、风险与量化门槛见 [§14 §4](./14.决赛优化状态与实施计划.md#4-活动实施计划)。

---

## 2. 已从本 Todo 移出的旧项

| 旧项 | 新状态 | 原因 |
|------|:------:|------|
| `2ec06f0` UPDATE_DELTA 是否上线 | 🧊 已集成 | 当前 HEAD 已包含，且已有约 +3.3% 本地长窗证据；不再是假想候选 |
| `31e5fac` SER 谓词预编译是否上线 | 🧊 已集成 | 当前 HEAD 已包含；排名固定 SI，不继续投入 |
| S5.2 固定 `sleep_for` | ❌ | OJ 明确负收益，永久退出活动队列 |
| `RMDB_SCAN_FASTPATH` | ❌ | 已发现 gap-EQ 漏行，默认关闭 |
| 1GB BPM | ❌ 决赛配置 | 当前 768MB 已接近 2GiB 峰值；不得恢复 |
| S5.3 `page_lsn`、S7.2、多活跃插入页 | ⏸ | 没有当前瓶颈证据；保留触发式计划 |
| yacc 重入、线程池、CBO、Arena/SLRU、per-table atomic | ⏸ | 当前排名路径收益低；触发条件见 §14 §5 |

历史设计和失败复盘仍保留在 [§3](./3.方案A-C优化记录与tpmC评估.md)、[§4](./4.S5-WAL组提交深化.md) 和 [§11](./11.WAL四项优化实现设计.md)，但不代表当前排期。

---

## 3. 每项固定验证流程

```bash
cd build && make rmdb -j$(nproc)
python3 tests/run_tests.py
python3 tests/local/consistency/run_consistency_regress.py

# 定向用例通过后，才跑趋势与决赛形：
python3 tests/local/run_oj_perf_test.py --mid
python3 tests/local/run_oj_perf_test.py --finals
bash tests/local/pre_oj_submit.sh
```

性能上线门槛：W=50 形中位提升 ≥5%，或异常 abort 相对下降 ≥20%；p99 不得恶化 >10%，任一测量轮不得退化 >2%。完整口径见 [§14 §6](./14.决赛优化状态与实施计划.md#6-统一验证方案)。

---

## 4. 完成后同步

- 活动项完成：移入 [§5 已完成优化总表](./5.已完成优化摘要.md)，写清代码位置与证据。
- 无显著收益：移入 [§6 冻结区](./6.搁置与低优先级方案.md)，保留数字和停止原因。
- 正确性或性能证伪：移入 §6 清退区，并从本文删除。
- 每次正式评测后更新 [决赛准备 Todo](../FinalCompetition/决赛准备Todo.md) 与 [OJ 成绩记录](./9.OJ成绩与提交记录.md)。
