# 6. 搁置与低优先级方案

> **用途**：未做或部分完成的优化备忘；优先级见 [§2 Todo](./2.待落地优化Todo.md)。  
> **已落地** → [§5](./5.已完成优化摘要.md) · **冻结** → [§12](./12.初赛冻结备忘.md)  
> **最后更新**：2026-07-11

---

## ⚠️ 部分完成

### 去掉 `buffer_mutex`（P3 / Todo T9）

- **已做**：`try_fast_parse_sql` 覆盖 TPC-C 热路径 DML。
- **未做**：yacc 回退路径仍 `pthread_mutex_lock(buffer_mutex)`。
- **代码**：`rmdb.cpp`

### MVCC 版本链后台 GC（P3 / Todo T10）

- **已做**：commit 路径同步 `prune_mvcc_after_commit`。
- **未做**：后台 GC 线程 / 惰性全表扫描。
- **代码**：`transaction_manager.cpp`

### `table_is_dirty` 进一步无锁化（P3 / Todo T13）

- **已做**：`any_mvcc_dirty_` 原子快路径 + `mvcc_dirty_mutex_` 共享锁。
- **未做**：per-table `atomic<bool>`。
- **代码**：`transaction_manager.cpp`、`executor_seq_scan.h`

### WAL P3 字节/等待者阈值组提交（Todo T1）

- **已做**：机制代码可开；**默认关**。
- **未做**：OJ A/B（须 `commit_waits/fsync≤2`）；初赛冻结。
- **代码**：`log_manager.*` · [§11 P3](./11.WAL四项优化实现设计.md)

---

## ❌ 未做（P3，TPC-C 当前收益低）

| 项 | 说明 | Todo |
|----|------|:----:|
| `record_locks_` 惰性回收 | 热行有限，tpmC 几乎无感 | T11 |
| 连接线程池 | TPC-C 长连接，`pthread_create` 可忽略 | T12 |
| Cost-based Join 重排 | Stock-Level 已靠 INLJ | T14 |
| Arena / SLRU 完整移植 | 待 malloc/evict 再成瓶颈 | T15 |
| S5.3 `page_lsn` 精确 flush | 写路径未维护 LSN | T6 |
| S7.2 `op_latch_` 分片 | 单槽 `cached_insert_page_` | T7 |

---

## ❌ 证伪（勿再默认启用）

| 项 | 结果 |
|----|------|
| S5.2 `sleep_for` 时间微批 | OJ ~950→~800；默认窗口 **0** |
| 表名硬编码热路径 | 赛方违规；2026-07-06 已删 |
