# 12. 初赛冻结备忘（历史档案）

> **冻结日**：2026-07-11 · **初赛已结束**（2026-07 下旬）  
> **性质**：初赛停刷榜决策与成绩锚点档案；**当前主线见** [`Docs/FinalCompetition/决赛准备Todo.md`](../FinalCompetition/决赛准备Todo.md)  
> 已落地分类总表 → **[§5](./5.已完成优化摘要.md)** · 决赛性能候选 → [§2](./2.待落地优化Todo.md)

---

## 1. 冻结点（初赛终局）

| 项 | 值 |
|----|-----|
| OJ 成绩 | **AC · 3615.5 tpmC**（独享机） |
| OJ 提交 | **`5a5527f`**（P0 统计 + P1 预分配/批帧） |
| 本地对照 | strict W=5/16 median **3513.26**（同日，≈OJ） |
| 本机远端 | 仅 **`gitlab`**（已去掉 gitee / github） |
| 本地 HEAD（冻结后继续） | `31e5fac`（+P2、+SER 谓词预编译） |

### 本地超前（相对 OJ 冻结点）

| 提交 | 说明 |
|------|------|
| `2ec06f0` | P2 `UPDATE_DELTA` 写路径 |
| `31e5fac` | SER 谓词条件预编译（`ser_compile_pred`） |

推 **`gitlab`** 不影响历史初赛 OJ 镜像分数。

---

## 2. 当时允许 / 禁止（冻结期内）

| 允许 | 禁止 |
|------|------|
| 改本地 `Docs/`、`notes/`、`AGENTS.md` | 为刷榜改 `src/` |
| 推 **gitlab** | 加回 gitee remote 交卷 |
| 本地回归自检 | 改默认 SER、改 `CMakeLists.txt` |
| 整理 exclude | 表名/SQL 硬编码旁路 |

> **决赛阶段**：允许为门禁与排名改 `src/`；仍禁硬编码旁路、禁改 CMake、禁为性能降 ACID。排名路径固定 **SI**。

---

## 3. 合规红线（持续有效）

禁止针对评测固定表名、字段或 SQL 文本写专用匹配并绕过通用解析/优化/执行。  
优化须落在通用索引、执行器、事务、缓存、WAL；不改 SQL 语义。  
当前 `src/` 无表名硬编码路径（见 [§5 §6](./5.已完成优化摘要.md)）。

---

## 4. 初赛结束后 → 决赛

主线已迁至 **[`Docs/FinalCompetition/决赛准备Todo.md`](../FinalCompetition/决赛准备Todo.md)**。  
赛题完整要点 → [`FinalCompetition/决赛赛题整理.md`](../FinalCompetition/决赛赛题整理.md)。

初赛收尾已完成项：

1. ~~修 C1 / C2 / H5 并纳入门禁~~ ✅；07-15 三轮 mid 均值 **~3970** / abort 0 → [§13](./13.P0正确性后性能复验收尾.md)  
2. 决赛增量（Wire v3 / SI·SER / COUNT(DISTINCT) / FLOAT32 / 可审计 fsync / W=50×32）→ 见决赛 Todo §1.2  
3. 性能候选 `2ec06f0` / `31e5fac` / T4 / T1 → 门禁通过后再评估（[§2](./2.待落地优化Todo.md)）

---

## 5. 仓库自检

```bash
git remote -v                          # 仅应有 gitlab
git log --oneline -1 HEAD
git status
git ls-files | rg -i 'Docs/|notes/|AGENTS|\.cursor'   # 应无输出（若走 exclude 策略）
```
