# 5. 已完成优化分类总表

> **用途**：初赛「已落地优化」权威分类（档案）；代码位置 + OJ/本地部署状态。  
> **决赛主线** → [`FinalCompetition/决赛准备`](../FinalCompetition/决赛准备Todo.md) · **数字** → [§3](./3.方案A-C优化记录与tpmC评估.md) · **OJ** → [§9](./9.OJ成绩与提交记录.md) · **锚点** → [§12](./12.初赛冻结备忘.md)  
> **最后更新**：2026-07-11（HEAD `31e5fac`；初赛 OJ 钉 `5a5527f` / **3615.5** tpmC）

---

## 0. 一页总览

| 层级 | 代表项 | OJ（`5a5527f`） | 本地 HEAD |
|------|--------|:---------------:|:---------:|
| 缓冲池 | 64 分片、1GB、Cleaner、S6 evict | ✅ | ✅ |
| WAL | 双缓冲组提交、S5.1、P0 统计、P1 预分配+批帧 | ✅ | ✅ |
| WAL | P2 `UPDATE_DELTA` byte-diff | ❌ | ✅ `2ec06f0` |
| 提交路径 | S3 unlock→fsync | ✅ | ✅ |
| MVCC | 分片 store、col_delta/col_patch、S1、meta 拆锁、overlay | ✅ | ✅ |
| MVCC | SER 谓词检测算法优化 | ❌ | ✅ `31e5fac` |
| 锁 | WFG、S2 `cv.wait` | ✅ | ✅ |
| 索引/查询 | S7.1 叶 pin、INLJ、fast-parse、SeqScan 预编译 | ✅ | ✅ |
| 合规 | 去表名硬编码（D2 等回退） | ✅ | ✅ |
| 证伪 | S5.2 `sleep_for` 微批（默认关） | 代码在 / 默认 0 | 同左 |

**成绩锚点**：OJ **3615.5**（独享机，`5a5527f`）· 本地 strict median **~3513**（同日）· 共享机时代峰值 ~950 / 基线 799.83。

---

## 1. 缓冲池 / 存储层

| 项 | 说明 | 代码 | 落地 |
|----|------|------|------|
| **方案 A：BPM 64 分片** | `page_table_` 接口保留；运行时 `shards_[]`；I/O 出锁 | `buffer_pool_manager.*`、`config.h` | ✅ OJ |
| **1GB 缓冲池** | `BUFFER_POOL_SIZE=262144` | `config.h` | ✅ OJ |
| **后台 Page Cleaner** | `free_frames_` 低于阈值时刷 `pin==0` 脏页 | `buffer_pool_manager.*`、`rmdb.cpp` | ✅ OJ |
| **S6：evict 分片** | 每分片 `free_frames_`；删全局 `free_list_`/`evict_latch_`；`fetch_page` CPU ~1% | `buffer_pool_manager.*`（`1b4e421`） | ✅ OJ |

---

## 2. WAL / 组提交 / 恢复

| 项 | 说明 | 代码 | 落地 |
|----|------|------|------|
| **双缓冲组提交** | `flush_worker`；仅有人 `wait_for_persist` 时 `fdatasync` | `log_manager.cpp` | ✅ OJ |
| **S5.1 锁拆分** | `append_mtx_` / `persist_mtx_` 分离 | `log_manager.{h,cpp}` | ✅ OJ |
| **S3：unlock→fsync** | commit 先放行锁再等持久化；本地 ~800→~1900+ 主因之一 | `transaction_manager.cpp` | ✅ OJ |
| **P0：WAL 管道统计** | `WalStats`；`RMDB_WAL_STATS=1` 打印 | `log_manager.{h,cpp}`（`5a5527f`） | ✅ OJ |
| **P1：预分配 + 批帧 CRC** | 16MB `ftruncate`；批头 magic/len/crc；恢复按批校验 | `disk_manager.*`、`log_manager.*`、`log_recovery.*`（`5a5527f`） | ✅ OJ |
| **P2：`UPDATE_DELTA`** | update 写路径 byte-diff 增量日志（通用，非表名特化） | `log_manager.h`、`executor_update.h`、`log_recovery.*`（`2ec06f0`） | ✅ 本地 / ❌ 未上 OJ |
| **P3 / S5.2+ 阈值组提交** | 字节/等待者阈值；代码可开，**默认关** | `log_manager.*` | 🧊 冻结 |
| **S5.2 时间微批** | `sleep_for`；OJ ~950→~800 **证伪**；`group_commit_window_us_=0` | `log_manager.*`（`9726c6a`） | ❌ 默认关 |

设计细节 → [§11](./11.WAL四项优化实现设计.md) · S5 复盘 → [§4](./4.S5-WAL组提交深化.md)

---

## 3. MVCC / 事务 / SSI

| 项 | 说明 | 代码 | 落地 |
|----|------|------|------|
| **MVCC 分片 store** | `mvcc_shards_[]` / `mvcc_shard_data_[]` 降锁竞争 | `transaction_manager.*` | ✅ OJ |
| **`col_delta` / `col_patch`** | 单列算术增量、多列 SET patch；按 AST+ColMeta，**无表名硬编码** | `executor_update.h`、`transaction_manager.*` | ✅ OJ |
| **S1：绝对 int/float → delta** | 单列赋值转 rebase 友好的 delta；district 热更新 profile ~31%→~9% | `executor_update.h` | ✅ OJ |
| **`hold_writer_to_commit`** | 跨语句累加增量，语句后 re-read 可见本事务写 | `transaction_manager.*` | ✅ OJ |
| **方案 C：meta 拆锁** | `rts_latch_` / `ser_latch_` / `mvcc_dirty_mutex_` | `transaction_manager.*` | ✅ OJ |
| **SI overlay** | `SiOverlayKey` 事务内可见性 | `transaction.h`、`transaction_manager.*` | ✅ OJ |
| **`any_mvcc_dirty_` 快路径** | 干净库跳过脏表检查；commit 同步 prune | `transaction_manager.cpp` | ✅ OJ |
| **SER 谓词检测优化** | 谓词读检查算法收紧（功能正确性 + 开销） | `transaction_manager.cpp`（`31e5fac`） | ✅ 本地 / ❌ 未上 OJ |
| **默认隔离 SER** | 会话默认勿改 SI（题九 SSI）；TPC-C 客户端发 SI | `rmdb.cpp` | ✅ 约束 |

合规泛化（去 `district`/`warehouse` 专用路径）→ [§3 §6](./3.方案A-C优化记录与tpmC评估.md#6-合规泛化改造2026-07-06)

---

## 4. 锁管理

| 项 | 说明 | 代码 | 落地 |
|----|------|------|------|
| **WFG 死锁检测** | wait-for 图；成环 abort **txn_id 最大**者；替代纯 wait-die 误杀 | `lock_manager.cpp`（`0fee051`） | ✅ OJ |
| **S2：`cv.wait`** | 替代 1ms 轮询；降 `cond_timedwait` CPU | `lock_manager.cpp` | ✅ OJ |

---

## 5. 索引 / 查询 / 解析

| 项 | 说明 | 代码 | 落地 |
|----|------|------|------|
| **S7.1：叶页持 pin** | `pinned_leaf_*` + `cached_leaf_no_`；quick ~1815→~2076 | `ix_index_handle.*` | ✅ OJ |
| **B+ `root_latch_` → shared_mutex** | 方案 C 一部分；并发读共享 | `ix_index_handle.*` | ✅ OJ |
| **INLJ** | Stock-Level 等等值连接走索引嵌套循环；NLJ 火焰图消失 | `planner.cpp`、`executor_index_nestedloop_join.h` | ✅ OJ |
| **fast-parse** | 通用 DML 形态手写解析 → 同构 AST → 仍走 analyze/optimize/execute | `rmdb.cpp` `try_fast_parse_*` | ✅ OJ |
| **SeqScan 预编译 + page 复用** | 条件预编译、页缓冲复用 | `executor_seq_scan.h` 等 | ✅ OJ |

---

## 6. 已回退 / 证伪（勿再启用）

| 项 | 原因 | 状态 |
|----|------|------|
| district / warehouse **表名硬编码**（D2、`mvcc_write_ytd_delta` 等） | 赛方禁止评测特化 | 已删除（2026-07-06） |
| S5.2 **`sleep_for` 默认 80μs** | OJ tpmC ~950→~800 | 默认改回 0（`9726c6a`） |
| 批量「洗注释」全库改写 | 痕迹过大 | 已撤销（2026-07-05） |

---

## 7. 成绩与里程碑（简表）

| 时间 | 事件 | tpmC |
|------|------|------|
| 2026-07-05 | OJ 合规基线（共享机） | **799.83** |
| 2026-07-06 | 方案 A–C（含后回退的表名版 B） | OJ ~1300 → 合规后本地 ~827 |
| 2026-07-08 | S1–S3 | 本地 strict median **~1909** |
| 2026-07-08/09 | S5.1+S6+S7.1 | 本地 quick **~2076**；共享机 OJ **~950** |
| 2026-07-09 | S5.2 默认 80μs 事故 → 回退 | OJ ~800 → 恢复 ~950 |
| 2026-07-10 | 独享机 + P0/P1（`5a5527f`） | **OJ 3615.5** ← **初赛冻结** |
| 2026-07-10+ | 本地 P2 + SER 谓词（未推 OJ） | HEAD `31e5fac` |

完整交卷记录 → [§9](./9.OJ成绩与提交记录.md)

---

## 8. 代码地图（按目录）

```
src/storage/          BPM 分片、Cleaner、S6；DiskManager 日志预分配
src/recovery/         组提交、S5.1、P0/P1/P2、恢复批帧
src/transaction/      MVCC 分片、col_delta、S3、SER/SSI、WFG 调用侧
src/execution/        Update col_delta/patch、SeqScan、INLJ
src/index/            S7.1 叶 pin、root shared_mutex
src/optimizer/        INLJ 规划
src/rmdb.cpp          fast-parse、会话隔离默认 SER
src/common/config.h   BUFFER_POOL_SIZE 等
```
